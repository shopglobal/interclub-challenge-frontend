import React from 'react';

import { StyledWrapper } from './styles';

import DetailsText from '../DetailsText/index';

export default () => (
  <StyledWrapper>
    <DetailsText primary text={'Transactions Summary'} />
  </StyledWrapper>
);
